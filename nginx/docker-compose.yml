services:
  nginx:
    image: nginx
    ports:
      - "8000:80"
    volumes:
      - "./backend/nginx/nginx.conf:/etc/nginx/nginx.conf"

  serverone:
    build: ./backend/serverOne
    ports:
      - "3001:3000"